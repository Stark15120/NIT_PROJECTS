v1 :
  this version referes to a very basic model of site 
  properties:
    basic : html and js use(dictionary)
  a front end only show a search option like work based on "key and value"

v2 :
  modify the display
  add more detail in json i mean 'n' number on a same name or key

v3:
  final code for the base version for AIFS
  that only uses the HTML / JS